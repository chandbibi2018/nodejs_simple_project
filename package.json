{
  "name": "nodejs_simple_project",
  "version": "1.0.0",
  "description": "sending two values from web page to database (PostgreSQL)",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "chandbibi",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2",
    "pg": "^7.4.1"
  }
}
